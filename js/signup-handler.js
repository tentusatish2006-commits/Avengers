/* Deprecated — signup.html has built-in handler. Keep file so old cache does not 404. */
(function () {
  'use strict';
  // Do not attach extra submit listeners (they blocked redirect before).
})();
