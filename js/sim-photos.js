/* Real NER field photos — pothole, landslide, flood */
if (typeof window !== 'undefined') {
  // Loaded from individual packs when available
  try {
    if (!window.SIM_PHOTO_POTHOLE && document.currentScript) { /* packs load separately */ }
  } catch (e) {}
}
